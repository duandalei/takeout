# 外卖管理系统 — 软件功能总体设计

---

## 一、系统总体层次方框图

系统采用 **分层架构**，自顶向下分为 4 层：**表现层 → 路由层 → 领域层 → 数据层**。

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
graph TD
    subgraph presentation["<b>🔹 表 现 层 (Presentation)</b>"]
        T1["Jinja2 模板<br/>17 个页面"] --- T2["Bootstrap 5<br/>响应式布局"] --- T3["Bootstrap Icons<br/>图标库"] --- T4["自定义 CSS<br/>style.css"]
    end

    subgraph routing["<b>🔹 路 由 层 (Routing)</b>"]
        R1["auth<br/>注册/登录/登出"] --- R2["restaurant<br/>商家店铺管理"] --- R3["menu<br/>菜品/分类管理"] --- R4["order<br/>订单/评价管理"] --- R5["delivery<br/>配送管理"]
    end

    subgraph domain["<b>🔹 领 域 层 (Domain)</b>"]
        D1["Authorization<br/>统一鉴权 @require"] --- D2["OrderState<br/>订单状态机 8 态流转"] --- D3["Forms<br/>WTForms 输入校验"]
    end

    subgraph data["<b>🔹 数 据 层 (Data)</b>"]
        M1["SQLAlchemy ORM<br/>8 个实体映射"] --- M2["pyodbc<br/>ODBC Driver 17"] --- M3["SQL Server<br/>TakeoutDB"]
    end

    presentation --> routing --> domain --> data

    style presentation fill:#e3f2fd,stroke:#1976d2,color:#000
    style routing fill:#fff9c4,stroke:#f9a825,color:#000
    style domain fill:#e8f5e9,stroke:#388e3c,color:#000
    style data fill:#f3e5f5,stroke:#7b1fa2,color:#000
```

> **图例**：🔵 表现层 — HTML 模板 + Bootstrap 5 前端　🟡 路由层 — Flask Blueprint 5 模块　🟢 领域层 — 鉴权 / 状态机 / 表单校验　🟣 数据层 — ORM → ODBC → SQL Server

### 层次职责说明

| 层次 | 技术 | 职责 | 关键约束 |
|------|------|------|----------|
| **表现层** | Jinja2 + Bootstrap 5 | 页面渲染、用户交互 | 不包含业务逻辑，只做展示与表单 |
| **路由层** | Flask Blueprint (5 模块) | HTTP 适配：解析请求 → 调用领域 → 渲染响应 | 不写业务代码，只做参数提取与调用转发 |
| **领域层** | Python 纯函数 + 类 | 鉴权校验、订单状态流转、表单规则 | 与 Flask 解耦，可脱离 HTTP 独立测试 |
| **数据层** | SQLAlchemy + pyodbc | 实体映射、查询执行、事务管理 | 7 表 + 1 查找表，13 个辅助索引 |

---

## 二、软件主要功能介绍

### 2.1 用户管理 — 统一认证体系

三种角色（顾客、商家、骑手）共用同一套注册/登录界面，通过 `role` 字段区分身份。

**角色权限矩阵：**

| 功能 | 顾客 | 商家 | 骑手 |
|------|:---:|:---:|:---:|
| 浏览商家/菜单 | ✅ | — | — |
| 下单 | ✅ | — | — |
| 管理店铺 | — | ✅ | — |
| 管理菜品 | — | ✅ | — |
| 处理订单（接单/备餐） | — | ✅ | — |
| 配送接单 | — | — | ✅ |
| 标记取餐/送达 | — | — | ✅ |
| 查看我的订单 | ✅ | ✅ | ✅ |
| 评价 | ✅ | — | — |

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart LR
    subgraph auth["<b>🔹 鉴权流程</b>"]
        direction TB
        A1["用户请求"] --> A2{"登录状态?"}
        A2 -->|"未登录"| A3["重定向 /login"]
        A2 -->|"已登录"| A4{"角色匹配?"}
        A4 -->|"不匹配"| A5["403 禁止访问"]
        A4 -->|"匹配"| A6{"资源归属?"}
        A6 -->|"非本人"| A5
        A6 -->|"本人/无需"| A7["✅ 放行"]
    end

    style A7 fill:#c8e6c9,stroke:#2e7d32,color:#000
    style A5 fill:#ffcdd2,stroke:#d32f2f,color:#000
```

> 鉴权由 `app/domain/auth.py` 中的 `@require(role=..., owns=...)` 装饰器统一处理，一次声明即完成：登录检查 → 角色校验 → 资源所有权验证。

---

### 2.2 商家管理 — 店铺与菜单维护

商家可以创建和编辑自己的店铺，管理菜品分类和菜品。

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart TD
    S1["商家登录"] --> S2["我的店铺"]
    S2 --> S3{"店铺存在?"}
    S3 -->|"否"| S4["创建店铺<br/>填写名称/地址/电话"]
    S3 -->|"是"| S5["店铺管理面板"]
    S4 --> S5
    S5 --> S6["编辑店铺信息"]
    S5 --> S7["管理菜品分类"]
    S5 --> S8["管理菜品"]
    S5 --> S9["营业/歇业切换"]
    S8 --> S10["添加菜品<br/>名称/描述/价格/分类"]
    S8 --> S11["编辑菜品信息"]
    S8 --> S12["上架/下架"]
    S8 --> S13["删除菜品"]

    style S5 fill:#e3f2fd,stroke:#1976d2,color:#000
    style S10 fill:#c8e6c9,stroke:#2e7d32,color:#000
    style S13 fill:#ffcdd2,stroke:#d32f2f,color:#000
```

---

### 2.3 订单管理 — 核心业务流程

订单是整个系统的核心，贯穿顾客、商家、骑手三方的协作。

**订单 8 种状态：**

| 阶段 | 状态 | 中文名 | 操作角色 |
|------|------|--------|----------|
| 阶段一 | `pending` | 待处理 | 顾客已提交 |
| 阶段一 | `confirmed` | 已确认 | 商家确认接单 |
| 阶段一 | `preparing` | 备餐中 | 商家开始制作 |
| 交接点 | `ready` | 待取餐 | 商家备好，等骑手 |
| 阶段二 | `assigned` | 配送中 | 骑手已接单 |
| 阶段二 | `picked_up` | 已取餐 | 骑手取餐完成 |
| 终态 | `delivered` | 已送达 | ✅ 完成 |
| 终态 | `cancelled` | 已取消 | 🚫 终止 |

**完整订单状态流转图：**

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart LR
    subgraph merchant["<b>🔹 阶段一：商家处理</b>"]
        direction LR
        A["① pending<br/>待处理"]
        B["② confirmed<br/>已确认"]
        C["③ preparing<br/>备餐中"]
        D["④ ready<br/>待取餐"]
        X1["✕ cancelled<br/>已取消"]

        A -->|"商家 确认接单"| B
        B -->|"商家 开始备餐"| C
        C -->|"商家 备餐完成"| D
        A -.->|"顾客/商家 取消"| X1
        B -.->|"顾客/商家 取消"| X1
    end

    subgraph rider["<b>🔹 阶段二：骑手配送</b>"]
        direction LR
        E["⑤ assigned<br/>配送中"]
        F["⑥ picked_up<br/>已取餐"]
        G["⑦ delivered<br/>已送达"]

        E -->|"骑手 到店取餐"| F
        F -->|"骑手 确认送达"| G
    end

    D -->|"骑手 抢单接单"| E

    style A fill:#e3f2fd,stroke:#1976d2,color:#000
    style B fill:#e3f2fd,stroke:#1976d2,color:#000
    style C fill:#fff9c4,stroke:#f9a825,color:#000
    style D fill:#fff9c4,stroke:#f9a825,color:#000
    style E fill:#e8f5e9,stroke:#388e3c,color:#000
    style F fill:#e8f5e9,stroke:#388e3c,color:#000
    style G fill:#c8e6c9,stroke:#2e7d32,color:#000
    style X1 fill:#ffcdd2,stroke:#d32f2f,color:#000
```

> 实线 = 正常流转　虚线 = 取消（旁路）　`preparing` 之后不可取消（防止食物浪费）　`delivered` / `cancelled` 为终态，不可再向后流转。

---

### 2.4 配送管理 — 骑手工作流

骑手独立的功能闭环：发现待接订单 → 接单 → 取餐 → 送达。

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart TD
    R1["骑手登录"] --> R2["查看待接订单列表<br/>status = 'ready'"]
    R2 --> R3{"选择订单"}
    R3 --> R4["抢单接单<br/>status → 'assigned'"]
    R4 --> R5["前往商家"]
    R5 --> R6["取餐确认<br/>status → 'picked_up'"]
    R6 --> R7["配送途中"]
    R7 --> R8["送达确认<br/>status → 'delivered'"]
    R8 --> R9["✅ 配送完成"]

    R2 --> R10["查看配送记录"]
    R10 --> R11["历史配送查询"]

    style R4 fill:#e8f5e9,stroke:#388e3c,color:#000
    style R6 fill:#e8f5e9,stroke:#388e3c,color:#000
    style R8 fill:#c8e6c9,stroke:#2e7d32,color:#000
    style R9 fill:#c8e6c9,stroke:#2e7d32,color:#000
```

---

### 2.5 评价系统

顾客在订单完成后可对订单进行 1-5 分评分和文字评价。一个订单只能评价一次（`order_id` 唯一约束）。

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart LR
    O["订单 delivered"] --> R{"已评价?"}
    R -->|"否"| R1["填写评分 1-5"]
    R -->|"是"| R2["查看已有评价"]
    R1 --> R3["填写文字评价(可选)"]
    R3 --> R4["提交评价"]
    R4 --> R5["✅ 评价完成"]

    style R5 fill:#c8e6c9,stroke:#2e7d32,color:#000
```

---

## 三、程序总体流程图

以下为系统的 **主控制流**：从用户请求到响应的完整路径。

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart TD
    Start(["🌐 浏览器请求"]) --> Route["Blueprint 路由匹配"]
    Route --> Auth{"@require 鉴权"}

    Auth -->|"未登录"| Login["重定向 /login"]
    Auth -->|"角色不匹配"| Forbidden["403 Forbidden"]
    Auth -->|"资源非本人"| Forbidden
    Auth -->|"鉴权通过"| Validate{"WTForms 表单校验"}

    Validate -->|"校验失败"| ErrPage["返回表单页<br/>显示错误提示"]
    Validate -->|"校验通过"| Domain["Domain 领域逻辑"]

    Domain --> D1{"操作类型"}
    D1 -->|"注册/登录"| OpAuth["用户认证<br/>scrypt 密码校验"]
    D1 -->|"CRUD"| OpCRUD["数据增删改查<br/>SQLAlchemy Session"]
    D1 -->|"状态流转"| OpState["订单状态机<br/>OrderState 转换"]

    OpAuth --> DB[("SQL Server<br/>TakeoutDB")]
    OpCRUD --> DB
    OpState --> DB

    DB --> Render["Jinja2 模板渲染"]
    Render --> Resp(["📄 HTTP 响应"])

    style Start fill:#1565c0,stroke:#0d47a1,color:#fff
    style Resp fill:#c8e6c9,stroke:#2e7d32,color:#000
    style Forbidden fill:#ffcdd2,stroke:#d32f2f,color:#000
    style DB fill:#f3e5f5,stroke:#7b1fa2,color:#fff
```

---

## 四、数据库模型图

系统共 **7 张业务表**，实体关系如下：

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '14px', 'primaryColor': '#4A90D9', 'primaryTextColor': '#fff', 'lineColor': '#555', 'fontFamily': 'Microsoft YaHei, sans-serif'}}}%%
flowchart LR
    U["Users<br/>用户"]
    R["Restaurants<br/>商家"]
    MC["MenuCategories<br/>菜品分类"]
    MI["MenuItems<br/>菜品"]
    O["Orders<br/>订单"]
    OI["OrderItems<br/>订单明细"]
    Rev["Reviews<br/>评价"]

    Rel1{"拥有<br/>1:N"}
    Rel2{"包含<br/>1:N"}
    Rel3{"包含<br/>1:N"}
    Rel4{"下单<br/>1:N"}
    Rel5{"包含<br/>1:N"}
    Rel6{"对应<br/>N:1"}
    Rel7{"对应<br/>1:1"}
    Rel8{"来自<br/>N:1"}
    Rel9{"配送<br/>N:1"}

    U -- "1" --> Rel1
    Rel1 -- "N" --> R

    R -- "1" --> Rel2
    Rel2 -- "N" --> MC

    MC -- "1" --> Rel3
    Rel3 -- "N" --> MI

    U -- "1" --> Rel4
    Rel4 -- "N" --> O

    O -- "1" --> Rel5
    Rel5 -- "N" --> OI

    OI -- "N" --> Rel6
    Rel6 -- "1" --> MI

    O -- "1" --> Rel7
    Rel7 -- "1" --> Rev

    Rev -- "N" --> Rel8
    Rel8 -- "1" --> U

    O -- "N" --> Rel9
    Rel9 -- "1" --> U
```




---

> 