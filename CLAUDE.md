## Agent skills

### Issue tracker

Issues 存放在 `duandalei/takeout` 的 GitHub Issues 中。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准标签使用默认名称（`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`）。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局 — 仓库根目录下由一个 `CONTEXT.md` + `docs/adr/` 组成。详见 `docs/agents/domain.md`。
